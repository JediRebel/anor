# ==== Backend ====
# NestJS 后端监听端口
BACKEND_PORT=4000

# 数据库连接字符串（Drizzle + PostgreSQL）
# 以后实际部署时会用真正的账号密码，这里只是模版
DATABASE_URL=postgres://username:password@localhost:5432/anor

# JWT 秘钥（生产环境一定要改成随机长字符串）
JWT_SECRET=change-me-in-production

# ==== Frontend ====
# Next.js 前端调用后端 API 的基础地址
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# 以后如果有前端用到的公共配置，也统一用 NEXT_PUBLIC_ 前缀
